// // Prevents additional console window on Windows in release, DO NOT REMOVE!!
// #![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

// use artifacts::models::{ChangePassword};
// #[cfg(feature = "specta")]
// use specta::Type;

// #[derive(Type)]
// pub struct A {
//     pub a: String,
// }

fn main() {
    // let idk: ChangePassword = ChangePassword::new("test".to_string(), "test".to_string());

    // dbg!(specta::export());

    // dbg!(specta_typescript::export::<ChangePassword>(&Default::default()).unwrap());

    // specta_typescript::Typescript::default()
    //     .export_to("./bindings.ts", &specta::export())
    //     .unwrap();
    // isekai_lib::run()
}
